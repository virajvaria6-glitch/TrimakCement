
  # Add Navigation Panel

  This is a code bundle for Add Navigation Panel. The original project is available at https://www.figma.com/design/Vo2S3wdT5qenC40zmlfIdy/Add-Navigation-Panel.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  